[{"title": "Mini Crackme: x86-64 (Windows)", "guid": "2026-08-14", "blurb": "Find the 32-bit value that must be passed in ecx for `check` to return 1.\n\ncheck:\n    mov     eax, ecx\n    xor     eax, 5Ah\n    cmp     eax, 1Ah\n    jne     fail\n    mov     eax, 1\n    ret\nfail:\n    xor     eax, eax\n    ret\n\nAnswer: ecx = 0x40 (64 decimal).\n\n1. eax is loaded with ecx, then XOR'd with 0x5A; the branch only avoids fail when that result equals 0x1A.\n2. So we need ecx ^ 0x5A == 0x1A. XOR is its own inverse, so ecx = 0x1A ^ 0x5A = 0x40.\n3. With ecx = 0x40, eax becomes 0x1A, the cmp/jne falls through, and eax is set to 1 before ret.", "link": ""}]
