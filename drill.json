[{"title": "Reverse to C: sum_sq (2026-09-22)", "guid": "2026-09-22", "blurb": "Target: Windows x86-64 (MSVC calling convention: RCX = arg1, EDX = arg2). Reverse this function to equivalent C.\n\nsum_sq:\n        push    rbp\n        mov     rbp, rsp\n        xor     eax, eax          ; total = 0\n        xor     r8d, r8d          ; i = 0\n.loop:\n        cmp     r8d, edx\n        jge     .done\n        movsxd  r9, dword ptr [rcx + r8*4]\n        imul    r9, r9\n        add     rax, r9\n        inc     r8d\n        jmp     .loop\n.done:\n        pop     rbp\n        ret\n\nWhat does sum_sq compute, and what are its parameter types?\n\n--- Answer ---\n\nlong long sum_sq(int *arr, int count) {\n    long long total = 0;\n    for (int i = 0; i < count; i++) {\n        long long v = arr[i];\n        total += v * v;\n    }\n    return total;\n}\n\nExplanation: push rbp / mov rbp, rsp / pop rbp is a standard stack-frame prologue and epilogue; this function has no locals or callees, so RBP is only saved and restored, never dereferenced. movsxd r9, [rcx + r8*4] reads a 32-bit int at scaled index i and sign-extends it into a 64-bit register before squaring, matching arr[i] widened to long long. cmp r8d, edx / jge .done is the loop guard (i < count), and RAX (return register) accumulates the running sum of squares.", "link": ""}]
