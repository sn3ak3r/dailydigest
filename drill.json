[{"title": "Mini Crackme: shl+add accumulator check", "guid": "2026-08-29", "blurb": "Challenge: rcx holds a pointer to a null-terminated ASCII string (Windows x64 calling convention). Find a string that makes `check` return 1.\n\ncheck:\n    xor     eax, eax          ; eax = accumulator\n    xor     edx, edx          ; edx = index\n.loop:\n    movzx   r8d, byte [rcx+rdx]\n    test    r8d, r8d\n    jz      .done\n    add     eax, r8d          ; acc += current byte\n    shl     eax, 1            ; acc <<= 1\n    inc     edx\n    jmp     .loop\n.done:\n    cmp     eax, 0x188\n    sete    al\n    movzx   eax, al\n    ret\n\nAnswer: \"AB\"\n\nEach loop iteration folds a byte into the accumulator with acc = (acc + c) << 1, a base-2 Horner scheme, so later characters get shifted further left than earlier ones. Tracing \"AB\" (0x41, 0x42): acc=0, then (0+0x41)<<1=0x82, then (0x82+0x42)<<1=0x188, matching the target. Any two-byte string c0 c1 with (c0<<1 + c1)<<1 == 0x188 also passes, so \"AB\" is one of several valid solutions.", "link": ""}]
