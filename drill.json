[{"title": "Mini Crackme: Bitwise NOT Check", "guid": "2026-08-26", "blurb": "Target: Windows x86-64, fastcall (first int arg in ecx).\n\nA \"license check\" function takes one byte (a character) in cl and returns 1 (pass) or 0 (fail). Find the single-character input that makes it return 1.\n\ncheck_char:\n    mov     eax, ecx        ; eax = input char\n    not     eax             ; bitwise complement (all 32 bits)\n    and     eax, 0FFh       ; keep only the low byte\n    cmp     eax, 0B4h       ; compare against target\n    jne     fail\n    mov     eax, 1\n    ret\nfail:\n    xor     eax, eax\n    ret\n\nWhat single ASCII character passed in cl makes check_char return 1?\n\n---\n\nAnswer: 'K' (0x4B / decimal 75)\n\n1. `not eax` flips every bit of the 32-bit register, so if ecx holds the input byte c, after not the low byte becomes (~c) & 0xFF (upper bits get masked off next anyway).\n2. The check requires (~c) & 0xFF == 0xB4. Solve backward: invert the target -- ~0xB4 & 0xFF = 0x4B -- since NOT is its own inverse.\n3. 0x4B is ASCII 'K'. Feeding 'K' in cl makes eax land on 0xB4, the jne falls through, and the function returns 1.", "link": ""}]
