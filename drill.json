[{"title": "Reverse to C: pointer-walk nonzero counter", "guid": "2026-09-01", "blurb": "Target: Windows x86-64 (MSVC calling convention: rcx, edx = arg1, arg2). Reverse this disassembly to equivalent C:\n\nfunc:\n    xor     eax, eax        ; eax = 0\n    test    edx, edx\n    jle     done\nloop_top:\n    cmp     dword ptr [rcx], 0\n    je      skip\n    inc     eax\nskip:\n    add     rcx, 4\n    dec     edx\n    jnz     loop_top\ndone:\n    ret\n\nWhat does func(int* arr, int n) compute, and what does it return?\n\n---\n\nAnswer:\n\nint func(int* arr, int n) {\n    int count = 0;\n    for (int i = 0; i < n; i++) {\n        if (arr[i] != 0) count++;\n    }\n    return count;\n}\n\n- rcx is walked as a raw pointer (add rcx, 4 advances by one int, 4 bytes) rather than indexed with a scaled base+offset -- each iteration dereferences [rcx] directly instead of [base + idx*4].\n- dec edx / jnz loop_top counts n down to zero instead of comparing an incrementing index, so the loop runs exactly n times with no explicit index variable.\n- cmp/je skip skips the inc eax for zero elements, so eax ends up holding the count of nonzero elements -- the function's return value per the Windows x64 convention.", "link": ""}]
