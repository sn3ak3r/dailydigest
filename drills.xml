<?xml version='1.0' encoding='UTF-8'?><rss version='2.0'><channel><title>Assembly Drill</title><link>https://example.com</link><description>Daily assembly drill</description><item><title>Reverse a Short Function to C: Bitwise Flag Test</title><link></link><description>Target: Windows x86-64 (fastcall: first int arg in ecx; return in eax).

Reverse this disassembly to equivalent C:

foo:
    mov     eax, ecx
    and     eax, 8
    setne   al
    movzx   eax, al
    ret

What does foo(int x) compute?

---

Answer:

int foo(int x) {
    return (x &amp; 8) != 0;
}

Explanation:
1. `and eax, 8` clears every bit of x except bit 3, leaving either 0 or 8, and sets ZF from that result.
2. `setne al` writes 1 into al if ZF is clear (the and was nonzero) or 0 if ZF is set, turning the flag test into a boolean without a branch.
3. `movzx eax, al` zero-extends that byte up to the full 32-bit eax return register, so stray high bits never leak into the result.</description><guid isPermaLink='false'>2026-08-25</guid><pubDate>Tue, 25 Aug 2026 02:37:40 +0000</pubDate></item></channel></rss>