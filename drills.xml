<?xml version='1.0' encoding='UTF-8'?><rss version='2.0'><channel><title>Assembly Drill</title><link>https://example.com</link><description>Daily assembly drill</description><item><title>Mini Crackme: length + sum check (2026-09-23)</title><link></link><description>Target: Windows x86-64 (MSVC calling convention: RCX = pointer to a null-terminated ASCII string). Find an input string that makes check return 1.

check:
        xor     r8d, r8d        ; r8d = length counter
        xor     r9d, r9d        ; r9d = sum accumulator
.loop:
        movzx   eax, byte ptr [rcx + r8]
        test    eax, eax
        jz      .end
        add     r9d, eax
        inc     r8d
        jmp     .loop
.end:
        cmp     r8d, 5
        jne     .fail
        cmp     r9d, 500
        jne     .fail
        mov     eax, 1
        ret
.fail:
        xor     eax, eax
        ret

What string(s) satisfy check?

--- Answer ---

&quot;ddddd&quot; (five copies of the character &#x27;d&#x27;) passes.

Explanation: The loop walks the string until the null terminator, counting bytes into r8d and summing their ASCII values into r9d, so check requires exactly two things: length == 5 and byte-sum == 500. &#x27;d&#x27; is ASCII 0x64 = 100 decimal, so five &#x27;d&#x27; characters give length 5 and sum 5*100=500, satisfying both comparisons. Any other 5-character string whose bytes sum to 500 also works, since the check only constrains the total, not individual characters.</description><guid isPermaLink='false'>2026-09-23</guid><pubDate>Wed, 23 Sep 2026 02:47:36 +0000</pubDate></item></channel></rss>