<?xml version='1.0' encoding='UTF-8'?><rss version='2.0'><channel><title>Assembly Drill</title><link>https://example.com</link><description>Daily assembly drill</description><item><title>Reverse a Function: bsf Bit-Scan (2026-09-25)</title><link></link><description>Reverse this short function to equivalent C (Windows x86-64, ecx arg, eax return):

mystery:
    bsf     eax, ecx
    jnz     found
    mov     eax, -1
found:
    ret

What does mystery(unsigned int x) return?

--- Answer ---

int mystery(unsigned int x) {
    if (x == 0)
        return -1;
    for (int i = 0; i &lt; 32; i++) {
        if (x &amp; (1u &lt;&lt; i))
            return i;
    }
    return -1;
}

It returns the bit index of the lowest set bit in x (like ffs(x) - 1), or -1 if x is zero.

Explanation: bsf eax, ecx (Bit Scan Forward) scans ecx from bit 0 upward and writes the index of the first 1 bit into eax; if the source is zero, the result is undefined and the zero flag (ZF) is set. jnz found skips the fallback only when ecx was nonzero (ZF clear), since bsf sets ZF when the input is all zeros. The mov eax, -1 path handles the zero-input edge case explicitly, matching the classic &quot;no bits set&quot; sentinel returned by functions like ffs.</description><guid isPermaLink='false'>2026-09-25</guid><pubDate>Fri, 25 Sep 2026 02:31:15 +0000</pubDate></item></channel></rss>